* Remove *.pyc files: `make clean`

* Recreate virtual env.

* Update settings:
    - DJANGO_DOCUMENTATION_* section.

* $ ./manage.py migrate

* Update search:
    - Update static pages data: `./manage.py crawl_static_pages`
    - Rebuild search index: `./manage.py rebuild_index`
    - Check search

* `rel_project` now add to root, not './src'